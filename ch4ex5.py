class calculater:
    def __init__ (s, num1=0, num2=0):
        s.num1=num1
        s.num2=num2
        print("object initialized")
    def accepet (s):
        try:
            n1=n2="  "
            n1=int(input("enter first number"))
            n2=int(input("enter second number")
                   s.num1=n1
                   s.num2=n2
                   except (ValueError, NameError) as e:
                       print("Enter number only.....",n1,n2)
                       print(e)
    def sum(s):
        sum=s.num1+s.num2
        if sum>0:
            print("sum of two number is",sum);
            
